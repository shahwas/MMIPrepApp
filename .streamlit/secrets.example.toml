[auth]
# Replace the placeholders with values from your OIDC provider (e.g. Google).
# Do NOT commit real secrets to the repository. Configure them in Streamlit Cloud
# via the app's Settings → Secrets instead.
redirect_uri = "https://share.streamlit.io/<your-github-username>/<your-repo>/oauth2callback"
cookie_secret = "REPLACE_WITH_A_STRONG_RANDOM_STRING"
client_id = "REPLACE_WITH_CLIENT_ID"
client_secret = "REPLACE_WITH_CLIENT_SECRET"
server_metadata_url = "https://accounts.google.com/.well-known/openid-configuration"
[auth]
# Replace these with your provider's details and a strong random cookie_secret.
# Example for Google (default provider):
redirect_uri = "http://localhost:8501/oauth2callback"
cookie_secret = "REPLACE_WITH_STRONG_RANDOM_STRING"
client_id = "YOUR_GOOGLE_CLIENT_ID"
client_secret = "YOUR_GOOGLE_CLIENT_SECRET"
server_metadata_url = "https://accounts.google.com/.well-known/openid-configuration"

# Example named provider (Okta):
#[auth]
#redirect_uri = "http://localhost:8501/oauth2callback"
#cookie_secret = "REPLACE_WITH_STRONG_RANDOM_STRING"

#[auth.okta]
#client_id = "YOUR_OKTA_CLIENT_ID"
#client_secret = "YOUR_OKTA_CLIENT_SECRET"
#server_metadata_url = "https://{yourOktaDomain}/.well-known/openid-configuration"
